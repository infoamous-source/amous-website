# AMOUS 웹사이트 작업 내역
**작업일시:** 2026.02.10 14:19:27

---

## ✅ 완료된 작업

### 1. Admin 보안 강화 (Part 1) - 완료 ✅

**작업 일시:** 2026.02.09

#### 구현 내용
- **JWT 인증 미들웨어 생성** (`src/middleware.ts`)
  - 모든 `/admin/*` 경로에 대한 자동 인증 체크
  - 로그인 없이 접근 시 자동으로 `/admin/login`으로 리다이렉트
  - JWT 토큰 유효성 검증 (jose 라이브러리 사용)
  - `/admin/login`은 인증 예외 처리

- **로그인 페이지 리다이렉트 처리** (`src/app/admin/login/page.tsx`)
  - `useSearchParams`로 `redirect` 쿼리 파라미터 처리
  - 로그인 성공 후 원래 접근하려던 페이지로 자동 복귀

#### 배포 상태
- ✅ GitHub 커밋: `1449466 Add admin authentication middleware and login redirect`
- ✅ GitHub 푸시 완료
- ✅ Vercel 자동 배포 완료

#### 검증 결과
- ✅ 로그아웃 상태에서 `/admin` 접속 → `/admin/login?redirect=%2Fadmin`으로 리다이렉트 확인
- ✅ Middleware가 정상 작동하여 로그인 페이지가 첫 화면으로 표시됨

#### 관련 파일
- `src/middleware.ts` (신규 생성)
- `src/app/admin/login/page.tsx` (수정)
- `20260209_admin_security.md` (작업 문서)

---

### 2. Admin 버튼 추가 - 완료 ✅

**작업 일시:** 2026.02.10

#### 구현 내용
- **Header에 숨김 Admin 버튼 추가** (`src/components/Header.tsx`)
  - 위치: Header 우측 (문의하기 버튼 왼쪽)
  - 스타일: 작은 회색 톱니바퀴 아이콘 (12px)
  - 동작: 클릭 시 `/admin/login` 페이지로 이동
  - 반응형: 데스크톱에서만 표시 (`hidden lg:inline-flex`)
  - 접근성: 툴팁 "관리자", 스크린 리더 텍스트 제공

#### 디자인 특징
- 기본 색상: `text-gray-400` (회색, 눈에 띄지 않음)
- 호버 시: `text-navy-800` (네이비로 변경)
- 아이콘: Heroicons 톱니바퀴 (설정/관리 의미)
- 크기: `w-3 h-3`, `text-xs`, `px-2 py-1` (최소 크기)

#### 배포 상태
- ✅ GitHub 커밋: `c5a73f7 Add admin button to Header`
- ✅ GitHub 푸시 완료
- ✅ Vercel 자동 배포 완료

#### 관련 파일
- `src/components/Header.tsx` (수정)

---

### 3. 계열사 이미지 갤러리 기능 추가 - 완료 ✅

**작업 일시:** 2026.02.09

#### 구현 내용
- **Admin 페이지**: 이미지 갤러리 관리 기능
  - 최대 5개 이미지 업로드
  - 이미지 순서 변경 (드래그 앤 드롭)
  - 이미지 삭제
  - 16:9 비율 썸네일 표시

- **프론트엔드 섹션**: 4초 자동 롤링
  - AnimatePresence로 페이드 전환
  - 점(dot) 인디케이터로 현재 위치 표시
  - 클릭으로 특정 이미지로 이동 가능

#### 배포 상태
- ✅ GitHub 커밋: `5dfaabc Add affiliate image gallery and rolling feature`
- ✅ GitHub 푸시 완료
- ✅ Vercel 배포 완료

#### 관련 파일
- `src/app/admin/affiliates/page.tsx` (수정)
- `src/components/sections/AffiliatesSection.tsx` (수정)
- `src/lib/supabase.ts` (Affiliate 인터페이스에 images 추가)
- `add-affiliate-images.sql` (DB 스키마 수정 SQL)

---

### 4. 강사 등록 API 수정 - 완료 ✅

**작업 일시:** 2026.02.09

#### 구현 내용
- **UUID 자동 생성**: `crypto.randomUUID()`로 instructor.id 자동 생성
- **instructor_code 자동 생성**: 없으면 `INST{timestamp}` 형식으로 생성
- NOT NULL 제약조건 위반 방지

#### 배포 상태
- ✅ GitHub 커밋: `d581194 Fix instructor registration API`
- ✅ GitHub 푸시 완료
- ✅ Vercel 배포 완료

#### 관련 파일
- `src/app/api/instructors/route.ts` (수정)

---

## ⚠️ 진행 중 / 미완료 작업

### 1. 계열사 이미지 저장 문제 수정 - 🔴 긴급

**문제 상황:**
- 계열사 관리에서 이미지를 추가하고 "저장 완료" 메시지가 나옴
- 페이지를 나갔다가 다시 돌아오면 이미지가 사라짐 (초기화됨)

**원인 분석:**
```
코드는 정상 ← Admin 페이지 & API 모두 images 처리 구현됨
       ↓
Supabase 테이블에 images 컬럼이 없음!
       ↓
PostgreSQL이 존재하지 않는 컬럼 업데이트를 조용히 무시
       ↓
API는 200 OK 응답 → 클라이언트는 "저장 완료" 표시
       ↓
실제로는 DB에 저장 안 됨
       ↓
재조회 시 images = null → 빈 배열로 표시
```

**해결 방법:**
```sql
-- Supabase Dashboard → SQL Editor에서 실행 필요
ALTER TABLE affiliates
ADD COLUMN IF NOT EXISTS images TEXT[];

NOTIFY pgrst, 'reload schema';
```

**상태:**
- ❌ SQL 실행 대기 중 (사용자가 직접 실행 필요)
- ✅ 코드는 이미 정상 구현됨

**관련 파일:**
- `add-affiliate-images.sql` (실행 필요)

---

### 2. Services 페이지 에러 처리 개선 - 🟡 중요

**문제 상황:**
- 저장 실패 시 "저장 실패" 메시지만 표시
- 에러 원인을 알 수 없음 (디버깅 불가능)

**개선 계획:**

#### 2.1 API 수정 (`src/app/api/services/route.ts`)
**현재:**
```typescript
} catch {
  return NextResponse.json({ error: "서버 오류가 발생했습니다." }, { status: 500 });
}
```

**개선안:**
```typescript
} catch (error: any) {
  return NextResponse.json({
    error: error.message || "서버 오류가 발생했습니다.",
    details: error.details,
    hint: error.hint
  }, { status: 500 });
}
```

#### 2.2 Admin 페이지 수정 (`src/app/admin/services/page.tsx`)
**현재:**
```typescript
try {
  // ...
  if (res.ok) {
    setMessage("저장 완료!");
  }
} catch {
  setMessage("저장 실패");  // ← 너무 단순
}
```

**개선안:**
```typescript
try {
  // ...
  if (res.ok) {
    setMessage("저장 완료!");
  } else {
    const errData = await res.json().catch(() => ({}));
    setMessage(`저장 실패: ${errData.error || res.statusText}${errData.hint ? ` (힌트: ${errData.hint})` : ""}`);
    setTimeout(() => setMessage(""), 5000);
  }
} catch (error) {
  setMessage("네트워크 오류가 발생했습니다.");
  setTimeout(() => setMessage(""), 3000);
}
```

**상태:**
- ❌ 코드 수정 대기 중
- 📋 계획 문서 작성 완료

**관련 파일:**
- `src/app/api/services/route.ts` (수정 필요)
- `src/app/admin/services/page.tsx` (수정 필요)

---

### 3. Cases 페이지 에러 처리 개선 - 🟡 중요

**문제 상황:**
- Services와 동일한 문제 (에러 처리 불충분)

**개선 계획:**

#### 3.1 API 수정 (`src/app/api/cases/route.ts`)
- POST catch 블록 (line 34-36)
- PUT catch 블록 (line 59-61)
- DELETE catch 블록 (line 77-79)

**모두 동일한 패턴으로 수정:**
```typescript
} catch (error: any) {
  return NextResponse.json({
    error: error.message || "서버 오류가 발생했습니다.",
    details: error.details,
    hint: error.hint
  }, { status: 500 });
}
```

#### 3.2 Admin 페이지 수정 (`src/app/admin/cases/page.tsx`)
- handleSave 함수 (line 47-58)
- Services와 동일한 에러 처리 패턴 적용

**상태:**
- ❌ 코드 수정 대기 중
- 📋 계획 문서 작성 완료

**관련 파일:**
- `src/app/api/cases/route.ts` (수정 필요)
- `src/app/admin/cases/page.tsx` (수정 필요)

---

### 4. Admin 모바일 최적화 (Part 2) - ⏸️ 대기

**요구사항:**
- Admin Layout: 모바일 햄버거 메뉴 추가
- Admin Dashboard: 카드 그리드 반응형
- Site Content: 이미지 갤러리 반응형
- Instructors: 폼 및 테이블 반응형
- Affiliates: 추가 반응형 개선
- Services: 폼 반응형
- Cases: 카드 레이아웃 반응형

**상태:**
- ⏸️ Part 1 (보안) 완료 후 사용자 승인 대기 중
- 📋 상세 계획 작성 완료 (이전 계획 파일에 포함)

**관련 파일:**
- `src/app/admin/layout.tsx`
- `src/app/admin/page.tsx`
- `src/app/admin/site-content/page.tsx`
- `src/app/admin/instructors/page.tsx`
- `src/app/admin/affiliates/page.tsx`
- `src/app/admin/services/page.tsx`
- `src/app/admin/cases/page.tsx`

---

## 📋 작업 우선순위

### 긴급 (즉시 수행 필요)
1. **🔴 계열사 이미지 저장 문제** - Supabase SQL 실행
   - `add-affiliate-images.sql` 실행
   - 이것만 하면 이미지 저장 문제 즉시 해결됨

### 중요 (코드 개선)
2. **🟡 Services 에러 처리 개선**
   - API 응답에 상세 에러 정보 포함
   - Admin 페이지에서 에러 원인 표시

3. **🟡 Cases 에러 처리 개선**
   - Services와 동일한 패턴 적용

### 대기 중
4. **⏸️ Admin 모바일 최적화**
   - 사용자 승인 대기 중

---

## 🔗 주요 링크

| 항목 | URL |
|------|-----|
| **홈페이지** | https://amous-website.vercel.app |
| **Admin 로그인** | https://amous-website.vercel.app/admin/login |
| **Admin 대시보드** | https://amous-website.vercel.app/admin |
| **GitHub 저장소** | https://github.com/infoamous-source/amous-website |
| **Vercel 대시보드** | https://vercel.com/infoamous-source/amous-website |

---

## 📊 Git 커밋 내역

```bash
# 최근 커밋 (최신순)
c5a73f7 Add admin button to Header
1449466 Add admin authentication middleware and login redirect
5dfaabc Add affiliate image gallery and rolling feature
d581194 Fix instructor registration API
7db74fc Trigger redeploy for cache refresh
4c91f23 docs: 20260209 작업내역 정리
137319f fix: service_id 컬럼 제거 - service_ids 배열만 사용
```

---

## 🛠️ 기술 스택

- **Framework:** Next.js 14 (App Router)
- **Language:** TypeScript
- **Authentication:** JWT (jose library)
- **Middleware:** Next.js Middleware (Edge Runtime)
- **Database:** Supabase (PostgreSQL)
- **Storage:** Supabase Storage
- **Styling:** Tailwind CSS
- **Animation:** Framer Motion
- **배포:** Vercel (자동 배포)

---

## 📝 다음 단계

### 즉시 실행
1. **Supabase SQL 실행** (2분)
   ```sql
   ALTER TABLE affiliates ADD COLUMN IF NOT EXISTS images TEXT[];
   NOTIFY pgrst, 'reload schema';
   ```

2. **계열사 이미지 저장 테스트** (3분)
   - Admin → 계열사 관리
   - 이미지 3장 추가
   - 저장 후 페이지 나갔다 돌아오기
   - 이미지 유지 확인

### 코드 개선 (선택)
3. **Services 에러 처리 개선** (10분)
   - API 수정: `src/app/api/services/route.ts`
   - Admin 수정: `src/app/admin/services/page.tsx`

4. **Cases 에러 처리 개선** (10분)
   - API 수정: `src/app/api/cases/route.ts`
   - Admin 수정: `src/app/admin/cases/page.tsx`

5. **전체 테스트** (10분)
   - 모든 Admin 페이지에서 저장/로딩 확인
   - 에러 케이스 검증

---

## 🔍 문제 해결 가이드

### 계열사 이미지가 저장되지 않을 때

**증상:**
- "저장 완료" 메시지가 나오는데 페이지 새로고침하면 사라짐

**해결:**
1. Supabase SQL Editor에서 `add-affiliate-images.sql` 실행
2. `NOTIFY pgrst, 'reload schema';` 반드시 실행
3. 브라우저 완전 새로고침 (Ctrl+Shift+R)
4. 다시 저장 시도

**검증:**
```sql
-- images 컬럼 존재 확인
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_name='affiliates' AND column_name='images';
```

### Admin 페이지 접근 안 될 때

**증상:**
- Admin 페이지 접속 시 계속 로그인 페이지로 이동

**해결:**
1. 비밀번호 확인: `amous2025admin!`
2. 쿠키 삭제 후 다시 로그인
3. JWT_SECRET 환경 변수 확인 (Vercel Dashboard)

### 배포가 반영 안 될 때

**증상:**
- 코드를 수정했는데 배포된 사이트에 반영 안 됨

**해결:**
1. Vercel Dashboard에서 배포 상태 확인
2. 배포 로그에서 에러 확인
3. 필요 시 수동 재배포 (Vercel Dashboard → Redeploy)

---

## 📚 참고 문서

- [Next.js 14 공식 문서](https://nextjs.org/docs)
- [Supabase 공식 문서](https://supabase.com/docs)
- [Tailwind CSS 공식 문서](https://tailwindcss.com/docs)
- [Framer Motion 공식 문서](https://www.framer.com/motion/)

---

## ✅ 작업 요약

**완료:**
- ✅ Admin 보안 강화 (JWT 미들웨어, 리다이렉트)
- ✅ Admin 버튼 추가 (Header 우측 톱니바퀴 아이콘)
- ✅ 계열사 이미지 갤러리 기능
- ✅ 강사 등록 API 수정

**진행 중:**
- 🔴 계열사 이미지 저장 문제 (SQL 실행 대기)
- 🟡 Services/Cases 에러 처리 개선 (코드 수정 대기)

**대기:**
- ⏸️ Admin 모바일 최적화 (승인 대기)

---

**작성자:** Claude Sonnet 4.5
**작성일:** 2026.02.10 14:19:27
